<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HTMLerning"
        tools:targetApi="31">
        <activity
            android:name=".AnimActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SecondActivity"
            android:label="Second Activity">
            <!-- Дополнительные настройки активности, если нужно -->
        </activity>
        <activity
            android:name=".SingInActivityEmail"
            android:label="Log Activity Email">
            <!-- Дополнительные настройки активности, если нужно -->
        </activity>
        <activity
            android:name=".SingUpActivityEmail"
            android:label="Register Activity Email">
            <!-- Дополнительные настройки активности, если нужно -->
        </activity>
        <activity
            android:name=".GeneralActivity"
            android:label="General Activity">
            <!-- Дополнительные настройки активности, если нужно -->
        </activity>
    </application>

</manifest>